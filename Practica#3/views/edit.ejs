<!DOCTYPE html>
<html>
<head>
  <title>Editar Libro</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <h2 class="my-4">Editar Libro</h2>
  <form method="POST" action="/edit/<%= libro.id %>">
    <div class="mb-3">
      <input name="titulo" class="form-control" value="<%= libro.titulo %>" required>
    </div>
    <div class="mb-3">
      <input name="autor" class="form-control" value="<%= libro.autor %>" required>
    </div>
    <div class="mb-3">
      <input name="genero" class="form-control" value="<%= libro.genero %>" required>
    </div>
    <div class="mb-3">
      <input name="anio_publicacion" type="number" class="form-control" value="<%= libro.anio_publicacion %>" required>
    </div>
    <button type="submit" class="btn btn-primary">Actualizar</button>
    <a href="/" class="btn btn-secondary">Volver</a>
  </form>
</div>
</body>
</html>
